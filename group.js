import Table from './table.js';

export default class Group {
    
    // name of the group
    name = '';
    // three rounds for each group
    rounds = [];
    // one table instance
    table = null;

    constructor() {
        
    }
}